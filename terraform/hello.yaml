swagger: "2.0"
info:
  title: cloudruntest
  description: "Test Cloud Run Function."
  version: "1.0.0"
schemes:
  - "https"
x-google-backend:
  address: https://hello-474907728177.europe-west2.run.app
produces:
- application/json
paths:
  "/hello":
    get:
      description: "Say hello"
      operationId: "hello"
      parameters:
        -
          name: name
          in: query
          required: true
          type: string
      responses:
        200:
          description: "Success."
          schema:
            type: string
        400:
          description: "The name is invalid or missing."
